<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.papa.dao.CmsPrefrenceAreaProductDAO">
    <insert id="insertList">
        insert into `cms_prefrence_area_product_relation`(`prefrence_area_id`,`product_id`)
        values
        <foreach collection="list" separator="," item="it" index="index">
            (
                #{it.prefrenceAreaId,jdbcType=BIGINT},
                #{it.productId,jdbcType=BIGINT}
            )
        </foreach>
    </insert>
</mapper>